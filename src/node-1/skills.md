# Node: node-1 (Worker Node)

## Role

Worker node operating behind NAT/LAN. Registers with gateway (node-0) on startup
and polls for jobs. Cannot be reached directly from outside the network.

## Capabilities

_Actions defined in node-1/actions/ — populated by LLM bootstrap workflow._
