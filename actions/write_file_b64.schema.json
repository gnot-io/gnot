{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "write_file_b64",
  "description": "Decode Base64 content and write to a file (binary-safe counterpart of read_file_b64).",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "minLength": 1,
      "description": "Destination file path. Parent directories are created automatically."
    },
    "content_b64": {
      "type": "string",
      "minLength": 1,
      "description": "Base64-encoded file content, as returned by read_file_b64."
    }
  },
  "required": ["path", "content_b64"],
  "additionalProperties": false
}
